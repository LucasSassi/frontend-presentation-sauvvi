import { UserDetail } from "../../../../entities/user/ui/UserDetail";

describe("UserDetail", () => {
  it("deve exportar o componente de detalhes da entity", () => {
    expect(UserDetail).toBeDefined();
  });
});
