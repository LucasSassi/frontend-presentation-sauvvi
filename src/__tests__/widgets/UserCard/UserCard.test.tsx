import { UserCardWidget } from "../../../widgets/UserCard/UserCard";

describe("UserCardWidget", () => {
  it("deve exportar o widget que une feature e entity", () => {
    expect(UserCardWidget).toBeDefined();
  });
});
