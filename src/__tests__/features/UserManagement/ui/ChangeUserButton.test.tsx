import { ChangeUserButton } from "../../../../features/UserManagement/ui/ChangeUserButton";

describe("ChangeUserButton", () => {
  it("deve exportar o botao da feature de troca de usuario", () => {
    expect(ChangeUserButton).toBeDefined();
  });
});
